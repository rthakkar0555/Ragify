"""
Document repository — data access layer for ingestion module.
"""

# TODO: Implement SQLAlchemy async repository
# - create(...)
# - get_by_id(document_id, tenant_id)
# - list_paginated(tenant_id, page, page_size)
# - update_status(document_id, status)
# - delete(document_id, tenant_id)
