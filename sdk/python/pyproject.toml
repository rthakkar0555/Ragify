[project]
name = "ragify-sdk"
version = "0.1.0"
description = "Python SDK for RAGify — RAG-as-a-Service Platform"
requires-python = ">=3.9"
dependencies = ["httpx>=0.26.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "pytest-httpx>=0.28.0"]
